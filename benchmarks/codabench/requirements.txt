# Competition runtime dependencies
# Install with: pip install -r requirements.txt

# Atomistic simulation environment (structure I/O, relaxation)
ase>=3.22

# Scoring utilities
scipy>=1.10
numpy>=1.24

# Model backends are intentionally split because MACE requires e3nn==0.4.4
# while FairChem/HydraGNN require e3nn>=0.5. Install requirements-mace.txt or
# requirements-fairchem.txt in the corresponding process environment.
